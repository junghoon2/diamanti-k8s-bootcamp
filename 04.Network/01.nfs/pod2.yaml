apiVersion: v1
kind: Pod
metadata:
  name: pod2
spec:
  containers:
    - image: busybox
      name: busy-container1
      command: ["/bin/sleep","7d"]
      volumeMounts:
        - name: vol2
          mountPath: /data
  volumes:
    - name: vol2
      persistentVolumeClaim:
        claimName: nfs-pvc
