apiVersion: v1
kind: Pod
metadata:
  name: pod1
spec:
  containers:
    - image: busybox
      name: busy-container1
      command: ["/bin/sleep","7d"]
      volumeMounts:
        - name: vol1
          mountPath: /data
  volumes:
    - name: vol1
      persistentVolumeClaim:
        claimName: nfs-pvc
