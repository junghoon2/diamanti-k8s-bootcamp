groups:
- name: maas

  rules:

  - alert: "maas-ZK Down-QStVG8IZ"
    expr: "(((sum(rate(process_cpu_seconds_total{job='maas-zookeeper'}[10s])) or  vector(0))) == bool 0 ) == 1"
    for: 5m
    labels:
      severity: 1
      action: 10
    annotations:
      summary: "maas:: ZK Down"


