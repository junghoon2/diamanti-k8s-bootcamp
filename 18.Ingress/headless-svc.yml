apiVersion: v1
kind: Service
metadata:
  labels:
    run: web
  name: web01
spec:
  selector:
    run: web01  # POD Label 지정
    # run: web01
  clusterIP: None
  type: ClusterIP  
  ports:
  - name: port
    port: 80  # 80 port를 80 port로 redirect 
    protocol: TCP
    targetPort: 80
