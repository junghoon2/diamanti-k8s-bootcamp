apiVersion: v1
kind: Service
metadata:
  labels:
    run: web
  name: web02
spec:
  selector:
    run: web02  # deploy가 아닌 POD의 Label 지정
    # run: web01
  clusterIP: None
  type: ClusterIP  
  ports:
  - name: port
    port: 80  # 80 port를 80 port로 redirect 
    protocol: TCP
    targetPort: 80
